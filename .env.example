# GitHub OAuth Worker URL
# Set this to your deployed Cloudflare Worker URL
# Example: https://github-oauth-worker.your-subdomain.workers.dev
VITE_OAUTH_WORKER_URL=

# Leave empty to disable OAuth (users will only see manual token input)
# When set, users will see a "Sign in with GitHub" button
